{
  "name": "@nightfall/keeper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nightfall AI player agents — off-chain service that fills empty seats with persona-driven LLM players (per-seat viewing keys).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "starknet": "10.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
